%p#notice= notice

%p
  %b Name:
  = @trip.name
  - @trip.members.each do |m|
    = m.name
    - m.expenses.each do |e|
      = e.comment
= link_to 'Edit', edit_trip_path(@trip)
\|
= link_to 'Back', trips_path
